import 'package:flutter/material.dart';

class RoastModel {
  int seconds;
  String stage;
  Color color;

  RoastModel({
    required this.seconds,
    required this.stage,
    required this.color,
  });
}